# Copy this to .env.local for local development

# PostgreSQL Database (Local)
DATABASE_URL=postgresql://mcp_user:mcp_password@localhost:5432/mcp_dev

# Redis for SSE transport (Local)
REDIS_URL=redis://localhost:6379

# MCP Server Configuration
MCP_API_KEY=your-secure-api-key-here

# For testing scripts (optional - can use command line args instead)
MCP_PRODUCTION_URL=https://your-vercel-app.vercel.app

# Production Examples:
# Neon (PostgreSQL): DATABASE_URL=postgresql://user:pass@ep-xxx.us-east-1.aws.neon.tech/db?sslmode=require
# Railway: DATABASE_URL=${{Postgres.DATABASE_URL}}
# Upstash Redis: REDIS_URL=redis://default:pass@region.upstash.io:port
